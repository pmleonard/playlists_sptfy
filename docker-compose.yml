services:
  web:
    build:
      context: .
      dockerfile: web/Dockerfile
    ports:
      - "5000:5000"
    volumes:
      - ./data:/data:rw
    environment:
      - DATA_ROOT=/data
      - FLASK_ENV=development
