services:
  web:
    build:
      context: .
      dockerfile: web/Dockerfile
    user: "1000:1000"
    ports:
      - "5000:5000"
    volumes:
      - ./data:/data:rw
    environment:
      - DATA_ROOT=/data
      - FLASK_ENV=development
